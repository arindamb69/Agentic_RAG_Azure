# ============================================
# Azure Authentication
# ============================================
# DefaultAzureCredential will automatically pick these up if set.
# Recommended for local development if you don't want to use 'az login'.
AZURE_TENANT_ID="" #Volvo Tenant Id
AZURE_CLIENT_ID="" # Service Principal Application ID
AZURE_CLIENT_SECRET="" # Service Principal Secret

# Azure LLM (GenAIHub API)
AZURE_OPENAI_ENDPOINT="https://api.volvogenaihubqa.volvogroup.net/openai/20250401/chat/completions"
AZURE_OPENAI_API_KEY="" # GenAIHub subscription API Key
AZURE_OPENAI_DEPLOYMENT="gpt-5.1"
AZURE_OPENAI_API_VERSION="20250401"
AZURE_OPENAI_TTS_DEPLOYMENT="tts"


# Search Provider Configuration
# Options: 'azure' (default) or 'custom' (Volvogen AI Hub)

# Azure AI Search (Vector Store)
AZURE_SEARCH_ENDPOINT=""
AZURE_SEARCH_KEY=""
AZURE_SEARCH_INDEX=""

SEARCH_PROVIDER="custom"
CUSTOM_SEARCH_URL="https://api.volvogenaihubqa.volvogroup.net/searchindex"
CUSTOM_SEARCH_API_KEY="" # GenAIHub Subscription API Key

# Database & Memory
COSMOS_CONNECTION_STRING="AccountEndpoint=https://<your cosmos account>.documents.azure.com:443/;AccountKey=<your cosmos account key>;"
COSMOS_DATABASE="" #Database name for memory
COSMOS_COLLECTION="" #Collection name for memory collection
COSMOS_DATA_DATABASE="" #Database name for non-memory data

# ============================================
# Azure Cache for Redis
# ============================================
# REDIS_HOST: Found in Azure Portal → Redis resource → Overview → "Host name"
#             Example: your-cache.redis.cache.windows.net
#
# For Entra ID authentication (recommended):
#   - REDIS_USE_ENTRA_ID=true
#   - REDIS_PASSWORD = Your Entra ID Object ID (Azure Portal → Entra ID → Users → Your user → Object ID)
#   - Enable "Microsoft Entra Authentication" in Redis → Settings → Authentication
#   - Add your identity under Redis → Settings → Data Access Configuration
#
#
REDIS_HOST="<Your REDIS Host Name>.redis.cache.windows.net"
REDIS_PORT=6380
REDIS_PASSWORD=""
REDIS_SSL=true
REDIS_USE_ENTRA_ID=true

# MCP Connectors. (Need to use APP credentials with Graph API)
SHAREPOINT_SITE_URL=""
SHAREPOINT_CLIENT_ID=""
SHAREPOINT_CLIENT_SECRET=""

#Confluence
CONFLUENCE_URL="https://api.volvogenaihubqa.volvogroup.net/confluence/mcp"
CONFLUENCE_SUBSCRIPTION_KEY="" # GenAIHub Subscription API Key

# Blob Storage (Azure Blob Storage)
# For Entra ID auth: set BLOB_ACCOUNT_URL (e.g., https://<storage-account>.blob.core.windows.net)
# For connection string auth: set BLOB_CONNECTION_STRING
BLOB_ACCOUNT_URL="https://<Your storage account>.blob.core.windows.net/"
BLOB_CONNECTION_STRING="DefaultEndpointsProtocol=https;AccountName=<Your storage account>;AccountKey=<Your storage account key>==;EndpointSuffix=core.windows.net"

# SQL Database (Entra ID Auth)
# Example of Entra ID MSI connection string
SQL_CONNECTION_STRING="Driver={ODBC Driver 18 for SQL Server};Server=tcp:<your-server>.database.windows.net,1433;Database=your-database;Encrypt=yes;TrustServerCertificate=no;Connection Timeout=30;Authentication=ActiveDirectoryMsi"
